NOTION_TOKEN=
FORMSPREE_CONTACT_ID=
FORMSPREE_GRIEVANCE_ID=

# Art Log — local dev only (set the same password you used in Cloudflare Pages → Settings → Bindings)
# This goes in .dev.vars, not .env — listed here for reference only
# ART_LOG_PASSWORD=
